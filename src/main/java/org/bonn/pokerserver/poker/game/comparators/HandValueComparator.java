package org.bonn.pokerserver.poker.game.comparators;

import org.bonn.pokerserver.poker.game.entities.HandValue;

import java.util.Comparator;

public class HandValueComparator implements Comparator<HandValue> {


    @Override
    public int compare(HandValue handValue, HandValue t1) {
        if (handValue.)
    }
}
